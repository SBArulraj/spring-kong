_format_version: "3.0"
_transform: true

services:
  - name: spring-boot-service
    url: http://spring-app:8080
    routes:
      - name: spring-boot-route
        paths:
          - /
        strip_path: false
        methods:
          - GET