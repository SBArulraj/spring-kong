_format_version: "3.0"
_transform: true

services:
  - name: spring-boot-service
    url: http://spring-app:8080
    routes:
      - name: spring-boot-route
        paths:
          - /api
        strip_path: true

plugins:
  - name: rate-limiting
    config:
      minute: 10
      policy: local
